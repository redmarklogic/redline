[pytest]
markers =
    connect: spawns claude mcp list / MCP handshake probes (slow)
    behavioral: runs headless claude -p sessions (slow, costs tokens)
addopts = -m "not connect and not behavioral" -q
