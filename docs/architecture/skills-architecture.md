# Skills Architecture — Placement Rulebook

> **SOT for layer assignments (L0–L9).** This document is the single authoritative source
> for skill-to-layer classification. The `layer` field in `skills-lock.json` is derived
> from the Layer Definitions section below via `hooks/sync-layer-to-lock.py`.
>
> **Owner (steward):** Peter (Principal Engineer). Layer reclassification requires
> sign-off from all agents in the skill's `owner_agent` (ADR-001).
>
> **Read this document when authoring, moving, or refactoring a skill** — it answers
> "where does this skill sit and what may it reference". It deliberately answers nothing
> else:
>
> - *Which skill do I load for a task?* → [docs/people/skills-taxonomy.md](../people/skills-taxonomy.md)
> - *Who owns or uses a skill?* → `skills-lock.json` (`owner_agent`) and the agent JDs in `.claude/agents/`
> - *What is missing, pending, or deprecated?* → [docs/people/skills-gaps.md](../people/skills-gaps.md)
> - *Why layers? Worked examples and onboarding prose* → [docs/knowledge/software-engineering/skills-system.md](../knowledge/software-engineering/skills-system.md)
>
> Agent–skill topology visuals are **not maintained here** (or anywhere) as a living
> document. Topology snapshots are generated from `skills-lock.json` during
> `hr-sync-agent-topology` runs and embedded, dated, in that sync's report.

---

## Principles

### 1. Dependency Direction

A skill at layer N may reference skills at layers 0 through N. It must **never** reference a skill at layer N+1 or above.

```text
Layer 6  ─────────────────────────────────────
  can reference →  Layer 5, 4, 3, 2, 1, 0

Layer 2  ─────────────────────────────────────
  can reference →  Layer 1, 0
  CANNOT reference →  Layer 3, 4, 5, 6, ...
```

### 2. Stability Gradient (lower = more stable)

Lower layers change less often. Upper layers are more volatile. Skills that change frequently belong in upper layers; stable standards and adapters belong near the foundation.

### 3. Vendor Boundary (Layer 0, immutable)

All vendor-maintained skills sit at Layer 0 (see the Layer 0 table below for the
authoritative list). They cannot reference project-owned skills — vendor updates
overwrite local modifications.

### 4. Single Source of Truth (registries at the bottom)

Foundation registries (`mental-models`) define concepts once. All other skills reference
their files — never redefine concepts inline.

### 5. Polyglot Before Language-Specific

Language-agnostic skills (data-tidy, security, git-version-control, mermaid-diagrams) sit
below language-specific skills (python-*). Python skills are implementations or
customisations of polyglot concepts. If a concept applies regardless of programming
language, it belongs in a lower layer.

### 6. Deep Modules at Layer Boundaries

Each layer exposes a minimal, stable interface upward. Prefer fewer powerful skills per
layer over many shallow ones that leak implementation details.

### 7. Horizontal Independence (within a layer)

Skills within the same layer may reference each other when logically necessary. This is
not a violation. The rule applies only to **vertical** dependencies: no upward references.

### 8. Placement Rule

When placing a new skill, ask:

> *"What is the highest-numbered layer containing all the skills this skill needs to reference?"*
> Place the new skill at that layer + 1 (or at that same layer if it references nothing).

Then add the skill to the correct table below **in the same commit** that creates the
skill folder, and run `python hooks/sync-layer-to-lock.py` to propagate the assignment
into `skills-lock.json`.

---

## Layer Definitions

The tables below are the authoritative layer assignments. One skill per row, alphabetical
within each layer. Purpose descriptions live in each skill's `SKILL.md` frontmatter and
in the catalog — **never here** (parallel definition is a defect, ADR-001).

### Layer 0 — Vendor Primitives (immutable)

**Rule**: No outbound references to project-owned skills. Modifications are overwritten on
vendor update.

The `speckit-*` command files in `.agents/skills/` are also vendor-managed (generated by
the `specify` CLI / installed as `.specify/extensions/`) but sit **outside layer
governance and outside `skills-lock.json`** — they are listed in the catalog for
discovery only and must never be edited manually.

| Skill | Source |
| --- | --- |
| `brainstorming` | `obra/superpowers` |
| `dispatching-parallel-agents` | `obra/superpowers` |
| `receiving-code-review` | `obra/superpowers` |
| `requesting-code-review` | `obra/superpowers` |
| `spec-kit` | `specify` |
| `subagent-driven-development` | `obra/superpowers` |
| `systematic-debugging` | `obra/superpowers` |
| `test-driven-development` | `obra/superpowers` |
| `using-git-worktrees` | `obra/superpowers` |
| `using-superpowers` | `obra/superpowers` |
| `verification-before-completion` | `obra/superpowers` |
| `writing-skills` | `obra/superpowers` |

### Layer 1 — Foundational Registries

**Rule**: No outbound references to any other skill. Pure reference registry.

| Skill |
| --- |
| `mental-models` |

### Layer 2 — Language-Agnostic Standards (polyglot)

**Rule**: May reference Layers 0–1.

| Skill |
| --- |
| `data-tidy` |
| `git-version-control` |
| `mermaid-diagrams` |
| `security` |
| `tool-selection` |

### Layer 3 — Platform Integrations (external-platform adapters)

**Rule**: May reference Layers 0–2.

**Definition**: Narrow, stable adapters to an external platform. The layer is defined by
this *role* — a thin boundary to an outside service — not by transport. A Layer 3 skill may
reach its platform via an MCP server, a CLI (e.g. `notebooklm-cli` uses the `nlm`
command), or a vendor SDK; the transport choice does not change the layer assignment.
Transport selection follows the CLI-first policy in ADR-016 (`docs/adr/adr-016-cli-first-tool-selection.md`).

| Skill |
| --- |
| `mcp-cce` |
| `miro-mcp` |
| `notebooklm-cli` |
| `python-mcp-tools` |
| `rag-prompting` |

### Layer 4 — Core Language Standards

**Rule**: May reference Layers 0–3.

| Skill |
| --- |
| `python-linting` |
| `python-paths` |
| `python-style` |
| `python-typing` |

### Layer 5 — Quality & Tooling

**Rule**: May reference Layers 0–4.

| Skill |
| --- |
| `dev-environment` |
| `python-deptry` |
| `python-performance` |
| `python-static-checks` |
| `python-testing-api` |
| `python-testing-unit` |
| `python-usethis` |
| `sonarqube-review` |
| `sonarqube-scan` |

### Layer 6 — Python Implementation (volatile)

**Rule**: May reference Layers 0–5.

| Skill |
| --- |
| `github-projects` |
| `python-class-design` |
| `python-crewai` |
| `python-data-ingestion` |
| `python-documentation` |
| `python-domain-modeling` |
| `python-error-handling` |
| `python-fastapi` |
| `python-function-design` |
| `python-module-structure` |
| `python-patterns` |
| `python-pins-data-version-control` |
| `python-plot-colors` |
| `python-script` |
| `python-script-numbering` |

### Layer 7 — Applied Capabilities

**Rule**: May reference Layers 0–6.

| Skill |
| --- |
| `eda-codebook` |
| `eda-interpreting-data` |
| `eda-qa` |
| `eda-visual-design` |
| `git-hooks-create` |
| `library-management` |
| `notebooklm-deep-research` |
| `notebooklm-index` |
| `qmd-narrative-design` |
| `qmd-tables` |
| `redline-research` |

### Layer 8 — Engineering Workflows

**Rule**: May reference Layers 0–7.

| Skill |
| --- |
| `adr-constitution-sync` |
| `create-adr` |
| `customization-mechanism-triage` |
| `define-ai-policy` |
| `design-eval-pipeline` |
| `design-eval-rubric` |
| `doc-updater` |
| `enforce-ai-batch-discipline` |
| `engineering-architecture` |
| `git-push-batched` |
| `prek-find-and-fix` |
| `resolving-pr-issues` |
| `session-handover` |
| `shaping` |
| `sonarqube-find-and-fix` |
| `task-defer` |
| `test-find-and-fix` |

### Layer 9 — Product, Strategy & Organisation

**Rule**: May reference Layers 0–8.

| Skill |
| --- |
| `agile-daily-standup` |
| `agile-sprint-planning` |
| `ceremony-monthly-editorial-session` |
| `ddd-strategic` |
| `hr-audit-agent` |
| `hr-hire-agent` |
| `hr-maintain-agent-registry` |
| `hr-sync-agent-topology` |
| `linkedin-social-selling` |
| `marketing-ai-content-review` |
| `marketing-content-big-5` |
| `marketing-product-led-seo` |
| `pm-decision-architect` |
| `pm-hypothesis-builder` |
| `pm-personas` |
| `pm-prd-builder` |
| `pm-prioritization` |
| `pm-problem-framer` |
| `pm-product-strategist` |
| `pm-roadmap` |
| `pm-structural-integrity-auditor` |
| `strategy-pre-mortem` |
| `strategy-psf-domain` |

---

## Vendor Sources

- [obra/superpowers](https://github.com/obra/superpowers) — general AI agent skills (brainstorming, subagent-driven-development, systematic-debugging, etc.)
- [github/spec-kit](https://github.com/github/spec-kit) — specification-driven development (`spec-kit` wrapper and the ungoverned `speckit-*` command files)
