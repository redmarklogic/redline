# Vendor Exclusions: Skill Compliance Remediation

**Feature**: 010-skill-compliance-remediation  
**Date**: 2026-05-30

## Excluded Skills

The following 4 skills are vendor-managed symlinks generated by the `specify` CLI. They are excluded from all remediation scope per FR-006 and the spec Assumptions.

| Skill | Reason |
|---|---|
| `speckit-shaping-gate-check` | Vendor symlink — overwritten by `specify upgrade` |
| `speckit-source-reconciliation-run` | Vendor symlink — overwritten by `specify upgrade` |
| `speckit-static-checks-run` | Vendor symlink — overwritten by `specify upgrade` |
| `speckit-verification-gate-run` | Vendor symlink — overwritten by `specify upgrade` |

All Redline-specific extensions for these skills must live in `.specify/extensions.yml`, not in the SKILL.md files themselves.

The editable skill count is **82** (86 total − 4 excluded).
