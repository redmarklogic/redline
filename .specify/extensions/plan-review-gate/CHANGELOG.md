## [1.0.0] - 2026-03-27

### Added
- Mandatory `before_tasks` hook that blocks `/speckit.tasks` unless `spec.md` and `plan.md` exist on the default branch
- `--skip-review` override flag to bypass the gate when needed
- Support for GitLab and GitHub remote detection
